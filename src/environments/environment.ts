import { AuthGuard } from 'totvs-log-web-foundation';

export const environment = {
  production: false,
  authGuard: AuthGuard,
  href: '',
  arquivoConfiguracao: 'appConfigDev.json',
  e2e: false,
  memoryDelay: 0
};
